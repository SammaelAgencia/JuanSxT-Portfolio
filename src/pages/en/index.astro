---
/** English home. Same composition as the Spanish one: every section reads the
 *  language from the route, so there is nothing to pass down. */
import Base from '@layouts/Base.astro';
import Home from '@components/Home.astro';
---

<Base>
  <Home />
</Base>
